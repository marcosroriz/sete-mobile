import { DefaultTheme } from "@react-navigation/native";

let orange = "FFA500";

export default SeteTheme = {
    ...DefaultTheme,
    colors: {
        ...DefaultTheme.colors,
        primary: orange,
    },
};
